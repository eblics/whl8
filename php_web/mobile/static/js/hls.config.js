(function(window) {

    var hls = window.hls || {};

    hls.config = {};

    // -----------------------------------
    // hls.config.Mobile
    hls.config.Mobile = {
        "base_url": "http://m.lsa0.cn"
    };

})(window);