   var mysql = require('./promise.js'); // or require('mysql2').createConnectionPromise
   mysql.createConnection({ debug: true, user: 'mycause_dev', password: 'mycause', rowsAsArray: true })
     .then((conn) => conn.query('select 1+1'))
     .then(([rows, fields]) => console.log(rows))
